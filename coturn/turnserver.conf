# Endereço IP que o TURN irá escutar
listening-ip=0.0.0.0

# IP Externo (seu IP da VPS)
external-ip=72.60.249.175

# Portas para STUN/TURN
listening-port=3478
min-port=49152
max-port=65535

# Desativa TLS/DTLS/TCP (simplifica para testes)
no-tls
no-dtls
no-tcp

# Autenticação
realm=livego.store
use-auth-secret
static-auth-secret=livego_sec_9x8y7z6a5b4c3d2e1f0g

# Otimizações
fingerprint
no-cli
no-stdout-log
log-file=/var/log/turn.log
user-quota=12
total-quota=1200
